#include <Arduino.h>

// Keep Arduino IDE compatibility by preserving the .ino sketch at the project
// root, while giving PlatformIO the src/main.cpp entrypoint it expects.
#include "../arduino_ace_slave.ino"
